- clean: ['~']

- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.bash_profile: bash_profile
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
